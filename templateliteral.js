 // here we use backtick (`) for representing temporal literals 
 //lets look an example

 let x =9;
 let y=7;
 let result = x+y;
 console.log("the addition of "+ x +" and " +y+ " is "+ result +"");

 //we want to print the addition of two numbers if the number is changed then it will automatically change the resulting statement

 //using temporal literals

 let a=7;
 let b=5;
 let res= a+b;
 console.log(`the addition of ${a} and ${b} is ${res}`);

 console.log(`athira
    is from                 
    kasaragod`)         //while using backtick there is no need to use \n for new line.we can simply write it down.